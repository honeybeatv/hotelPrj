<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.site.mapper.UserMapper">
  	
  	<!-- 로그인 userid,upw 검색 -->
  	<select id="selectLogin" resultType="com.site.vo.UserVo">
  		select * from user1 where userid=#{userid} and userpw=#{userpw} 
  	</select>
  	
  	<!-- 회원가입 -->
	<insert id="join_check">
	    insert into user1 values(#{name},#{userid},
	    #{userpw},SEQ_USER.nextval,#{uphone},0,#{uemail})
	</insert>
	
	<!-- 아이디 중복체크 -->
	<select id="id_check" parameterType="String" resultType="int">
	    select count(*) from user1 where userid=#{userid}
	</select>
  
	<!-- 회원 기본정보 호출 -->
  	<select id="selectUserInfoList" resultType="com.site.vo.UserVo">
	  	<![CDATA[
	  		SELECT * 
	  		  FROM user1 
	  		 WHERE userno=#{userno}
	  	]]>
  	</select>
  	
  	<!-- 회원 기본정보 수정 -->
  	<update id="updateUserInfoModifyDo">
	  	<![CDATA[
		  	UPDATE user1 
		  	   SET name=#{name}		, 
		  	   	   userid=#{userid}	, 
		  	   	   userpw=#{userpw}	, 
		  	   	   uphone=#{uphone}	, 
		  	   	   uadmin=#{uadmin}	, 
		  	   	   uemail=#{uemail}
		  	 WHERE userno=#{userno}
		]]>
  	</update>

	<!-- 회원 예약한 예약정보 호출 -->
	<select id="selectUserReservationViewList" resultType="com.site.vo.UserReservationVo">
		<![CDATA[
			 SELECT ro.rname as rname, ro.raddress as raddress, ro.rprice as rprice, ro.roomno as roomNo,
			 		to_char(re.startday, 'yyyy-MM-dd') as startday, to_char(re.endday, 'yyyy-MM-dd') as endday,
			 		re.status as status
			    FROM reserve re, room ro
			    WHERE re.roomno = ro.roomno
			    AND re.userno = #{userno}
		]]>
	</select>
  	
  	<!-- 회원 호스팅 상품정보 호출 -->
  	<select id="selectUserHostingViewList" resultType="com.site.vo.RoomVo">
  		<![CDATA[
	  		SELECT * 
	  		  FROM room 
	  		 WHERE userno=#{userno}
	  	  ORDER BY roomno asc
		]]>
  	</select>

  	<!-- 회원 호스팅 상품정보 수정 호출 -->
  	<select id="selectUserHostingModifyList" resultType="com.site.vo.RoomVo">
  		<![CDATA[
	  		SELECT * 
	  		  FROM room 
	  		 WHERE userno=#{userno}
	  		   and roomno=#{roomNo}
		]]>
  	</select>
  	
  	<!-- 회원 호스팅 상품정보 수정 실행 -->
  	<update id="updateUserHostingModifDo" >
  		<![CDATA[
		  	UPDATE room 
		  	   SET rname=#{rname}		, 
		  	   	   rcity=#{rcity}		, 
		  	   	   rtype=#{rtype}		, 
		  	   	   rpeople=#{rpeople}	, 
		  	   	   rprice=#{rprice}		, 
		  	   	   rbed=#{rbed}			,
		  	   	   rroom=#{rroom}		,
		  	   	   rsmoke=#{rsmoke}		,
		  	   	   rpet=#{rpet}			,
		  	   	   raddress=#{raddress}
		  	 WHERE roomno=#{roomNo}
		  	   and userno=#{userno}
	  	 ]]>
  	</update>
	<!-- 예약 승인 처리 -->
  	<update id="userHostingReservationApprove" >
  		<![CDATA[
		  	UPDATE reserve 
		  	   SET status= '승인',
					approvedate= sysdate
		  	 WHERE re_no=#{re_no}
	  	 ]]>
  	</update>
	<!-- 예약 거부 처리 -->
  	<update id="userHostingReservationReject" >
  		<![CDATA[
		  	UPDATE reserve 
		  	   SET status= '거부',
					approvedate= sysdate
		  	 WHERE re_no=#{re_no}
	  	 ]]>
  	</update>

  	<!-- 회원 호스팅 상품 삭제를 위한 예약내역 삭제 -->
  	<delete id="deleteUserReservationDeleteList">
	  	<![CDATA[
	  		DELETE reserve 
	  		 WHERE roomno=#{roomNo}
	  	]]>
  	</delete>
  	
  	<!-- 회원 호스팅 상품 삭제를 위한 예약내역 삭제 결과확인용 -->
  	<select id="selectUserReservationDeleteCount" resultType="int">
	  	<![CDATA[
			SELECT count(*) 
			  FROM reserve 
			 WHERE roomno=#{roomNo}
		]]>  	
  	</select>
  	
  	<!-- 회원 호스팅 상품 삭제 -->
  	<delete id="deleteUserHostingDeleteList">
	  	<![CDATA[
	  		DELETE room 
	  		 WHERE roomno=#{roomNo}
	  	]]>
  	</delete>
  	
  	<!-- 회원 호스팅 상품 삭제 결과확인용 -->
  	<select id="selectUserHostingDeleteCount" resultType="int">
	  	<![CDATA[
			SELECT count(*) 
			  FROM room 
			 WHERE roomno=#{roomNo}
		]]>  	
  	</select>
	<select id="HostingReservation" resultType="com.site.vo.ReserveVo">
	<![CDATA[ select a.name, a.uphone,b.re_no,b.status, to_char(b.startday, 'yyyy/MM/dd')as startday , to_char(b.endday, 'yyyy/MM/dd')as endday,b.request,to_char(b.reservedate, 'yyyy/MM/dd')as reservedate, to_char(b.approvedate, 'yyyy/MM/dd')as approvedate from user1 a,reserve b where a.userno =b.userno
and b.roomNo = #{roomNo} ]]> 
	</select>

  </mapper>