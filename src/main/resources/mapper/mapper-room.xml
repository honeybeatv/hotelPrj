<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.site.mapper.RoomMapper">
  
  	<select id="getlist" parameterType="String" resultType="com.site.vo.RoomVo">
	select distinct a.* from room a,reserve b where not in TO_DATE(#{start},'YYYYMMDD') between b.startday and b.endday and not in TO_DATE(#{end},'YYYYMMDD') between b.startday and b.endday
  	</select>

  </mapper>