<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.site.mapper.RoomMapper">
  
  	<select id="getlist" parameterType="String" resultType="com.site.vo.RoomVo">
	select distinct a.* from room a , reserve b where a.roomno = b.roomno and
	not TO_DATE (#{start},'YYYYMMDD') between b.startday and b.endday 
	and not TO_DATE(#{end},'YYYYMMDD') between b.startday and b.endday
  	</select>
  	
  	<!-- 상세 조건 검색 -->
  	<select id="selectAdvancedRoomList" resultType="com.site.vo.RoomVo">
		<![CDATA[
		SELECT distinct ro.* FROM reserve re, room ro
			WHERE re.roomno = ro.roomno
			AND NOT TO_DATE(#{checkIn}, 'YYYYMMDD') BETWEEN re.startday AND re.endday AND NOT TO_DATE(#{checkOut}, 'YYYYMMDD') BETWEEN re.startday AND re.endday
			AND ro.rtype=#{roomType}
			AND ro.rpeople >= 0
			AND ro.rroom >= #{bedroom}
			AND ro.rbed >= #{bed}
			AND ro.rsmoke = #{smoke}
			AND ro.rpet = #{pet}
			AND #{minPrice}<ro.rprice AND ro.rprice<#{maxPrice}
  		]]>
  	</select>
  

  </mapper>